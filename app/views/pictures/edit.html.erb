<% form_for([@user, @user.picture], :html => { :multipart => true }) do |f| %>
  <%= f.error_messages %>

  <b>Huom! Kuvien kokoraja on 512kB.</b>
  <p>
    <%= f.label "Kuvatiedosto " %><br />
    <%= f.file_field :uploaded_picture %>
  </p>
    <p>
    <%= f.submit "Lähetä" %>
  </p>
<% end %>

<%= link_to 'Takaisin', :back %>

