%h1 Login
= form_tag('create') do
  = field_set_tag do
    = label_tag :user_login, t('session.username')
    = text_field_tag :user_login, size: 20
    = label_tag :user_password, t('session.password')
    = password_field_tag :user_password, size: 20
    = submit_tag t('session.login')
